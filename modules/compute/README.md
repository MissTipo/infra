# compute

This module provisions compute resources, including OCI compute instances, instance configurations, and instance pools.
It enables scalable and configurable virtual machines used for workloads that do not rely on container orchestration.
