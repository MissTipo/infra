# network

This module provisions networking resources for the OCI environment.
It manages the Virtual Cloud Network (VCN), public and private subnets,
routing tables, gateways, and relevant network security configurations.
It establishes the foundational network layer that all other services depend on.
