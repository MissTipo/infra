# object-storage

This module defines Object Storage resources such as buckets, retention rules, pre-authenticated requests, and lifecycle configurations.
It supports durable and cost-efficient storage for artifacts, logs, backups, and unstructured data.
